const div = document.getElementById('app')
const header = document.createElement('h1')
const texto = 'Olá mundo !!'
const contentHeader = document.createTextNode(texto)
header.appendChild(contentHeader)
app.appendChild(header)